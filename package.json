{
  "name": "CAPBASIC",
  "version": "1.0.1",
  "description": "A simple CAP project.",
  "dependencies": {
    "@cap-js/hana": "^2",
    "@sap/cds": "^9",
    "@sap/xssec": "^4.11.0",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9"
  },
  "scripts": {
    "start": "cds-serve",
    "test": "echo \"No tests defined\" && exit 0"
  },
  "private": true,
  "cds": {
    "auth": {
      "kind": "xsuaa"
    }
  }
}